spring.application.name=tokonyadia

spring.datasource.url=jdbc:postgresql://localhost:5432/enigma_db
spring.datasource.username=andre
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show=true

# JWT configuration Secret
app.tokonyadiashop.jwt.jwt-secret = secret-key
app.tokonyadiashop.jwt.app-name = Tokonyadia App
app.tokonyadiashop.jwt.jwt-expired = 3000
